<h2><%= title 'Editar artículo' %></h2>
<%= render :partial => 'uploads/new' %>

<%= error_messages_for :article, :class => 'error' %>

<% form_for @article do |f| %>
  <%= render :partial => f %>
  <p class="clearfix">
    <%= button 'Guardar cambios', :class => :positive, :name => 'publish' %>
    <%= button 'Previsualizar', :name => 'preview' %>
    <%= link_button_to 'Cancelar', article_path(@article.permalink), :class => 'negative' %>
  </p>
<% end %>
